//import { ADD_CARD} from './ListActions';
//import { remove, edit, add } from '../../common/helpers';
import lists from '../../common/dataTest'

//TODO: replace the fixed state
const addcards = (state = 'atazftzfatz', action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default addcards;