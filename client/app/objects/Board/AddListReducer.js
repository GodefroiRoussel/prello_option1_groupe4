/*//import { ADD_CARD} from './ListActions';
//import { remove, edit, add } from '../../common/helpers';
import boardsTest from '../../common/dataTest'

//TODO: replace the fixed state
const addlists = (state = 'board', action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default addlists;*/